require 'fileutils'

irb_state_dir = File.join(ENV.fetch('XDG_STATE_HOME', File.join(Dir.home, '.local', 'state')), 'irb')
FileUtils.mkdir_p(irb_state_dir)
IRB.conf[:HISTORY_FILE] = File.join(irb_state_dir, 'history')
